# 贡献指南

感谢你愿意改进 **Bitcoin Flow Monitor**。本项目旨在用清晰、可审计的方式呈现比特币公开链上数据；贡献应优先提升准确性、可访问性、可靠性或使用体验。

## 开始之前

请先 Fork 本仓库，创建一个描述清楚的分支，并在本地安装依赖。提交前请运行以下命令，确保类型检查和单元测试均可通过。

```bash
pnpm check
pnpm test
```

| 贡献类型 | 建议做法 |
| --- | --- |
| 缺陷修复 | 在提交说明中描述复现步骤、预期行为与实际行为，并附上覆盖该问题的测试。 |
| 新功能 | 先开启 Issue 说明用户价值、数据来源和潜在隐私影响；避免引入需要密钥的链上数据源。 |
| 可视化改进 | 保持输入、交易、输出三类节点的视觉区分，并同时验证键盘可达性和小屏幕布局。 |
| 文档改进 | 同步更新 README 中的安装、使用或架构说明，并保证链接指向官方资料。 |

## 开发原则

项目仅使用 Blockstream.info 或 Blockchain.info 的公共 REST 接口。请勿添加私有、付费或需认证的数据端点。比特币 UTXO 交易无法仅凭公开图形确定最终受益人或找零输出；因此，所有文案与功能都不得将可视化表述为实体归属或支付意图的证明。

请使用 TypeScript，避免 `any`，并把数据转换逻辑放在服务端的独立模块中，以便使用 Vitest 覆盖。界面更新需兼顾浅色、深色主题以及键盘焦点状态。不要提交 `.env`、密钥、个人地址簿或从公共接口下载的大型数据快照。

## Pull Request

每个 Pull Request 应聚焦一项变更。请在说明中简述动机、实现方式、验证命令和可见 UI 影响。如修改 API 适配器，请列出受影响的接口路径；如修改资金流含义，请同步更新 README 的限制说明。维护者可能会要求你拆分无关改动、增加测试或补充可访问性说明。

本项目遵循尊重、建设性、以事实为依据的协作方式。提交贡献即表示你同意在 MIT License 条款下授权该贡献。
